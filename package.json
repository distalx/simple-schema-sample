{
  "name": "simple-schema",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.14.8",
    "jquery": "^3.6.0",
    "meteor-node-stubs": "^1.0.3",
    "simpl-schema": "^1.12.0"
  },
  "devDependencies": {
    "chai": "^4.2.0"
  }
}
